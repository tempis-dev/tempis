The default library (tempis-timeline) should just take a canvas context and some timeline options, nothing else.

We can make a react wrapper (react-tempis-timeline) later, which just renders a canvas and then uses the default library.